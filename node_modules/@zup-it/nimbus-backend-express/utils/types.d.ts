export interface NimbusHeaders {
    'nimbus-platform'?: 'WEB' | 'MOBILE';
}
export declare type IsRequired<T extends object, K extends keyof T> = T extends Record<K, T[K]> ? true : false;
export declare type HasRequiredProperty<T extends object> = {
    [K in keyof T]: IsRequired<T, K>;
} extends Record<string, false> ? false : true;
