import { FC, WithChildren } from '@zup-it/nimbus-backend-core';
export interface ScrollViewProps extends Required<WithChildren> {
    /**
     * @default 'both'
     */
    direction?: 'vertical' | 'horizontal' | 'both';
    /**
     * @default true
     */
    scrollIndicator?: boolean;
}
export declare const ScrollView: FC<ScrollViewProps>;
