import { FC, WithChildren } from '@zup-it/nimbus-backend-core';
import { BoxProps } from './types/box';
declare type Alignment = 'topStart' | 'topEnd' | 'bottomStart' | 'bottomEnd' | 'topCenter' | 'bottomCenter' | 'centerStart' | 'centerEnd' | 'center';
export interface PositionedProps extends Omit<BoxProps, 'children'>, Required<WithChildren> {
    /**
     * @default 'topStart'
     */
    alignment?: Alignment;
    /**
     * @default 0
    */
    x?: number;
    /**
     * @default 0
    */
    y?: number;
}
export declare const Positioned: FC<PositionedProps>;
export {};
