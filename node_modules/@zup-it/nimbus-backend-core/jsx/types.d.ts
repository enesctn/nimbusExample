import { Component } from '../model/component';
/**
 * Adds the common properties of a component to the type passed in the generic.
 *
 * Properties added: id
 */
export declare type ComponentProps<Props> = Pick<Component, 'id'> & Pick<Component, 'state'> & Props;
/**
 * A functional component. It receives the component properties and returns another JSX element.
 */
export declare type FC<Props = unknown> = (props: ComponentProps<Props>) => Component;
