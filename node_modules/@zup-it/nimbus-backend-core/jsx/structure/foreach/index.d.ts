import { Component } from '../../../api';
import { ForEachProps } from './types';
interface ForEachFC {
    <T>(props: ForEachProps<T>): Component;
}
export declare const ForEach: ForEachFC;
export {};
