import { Operation } from '../../api';
import { ValidOperationAttribute } from '../../model/operation/types';
export declare const eq: (a: ValidOperationAttribute, b: ValidOperationAttribute) => Operation<boolean>;
