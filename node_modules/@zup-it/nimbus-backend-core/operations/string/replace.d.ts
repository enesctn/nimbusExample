import { Operation } from '../../api';
import { Expression } from '../../types';
export declare const replace: (text: Expression<string>, oldTerm: Expression<string>, newTerm: Expression<string>) => Operation<string>;
