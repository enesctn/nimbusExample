import { Operation } from '../../api';
import { Expression } from '../../types';
export declare const substr: (text: Expression<string>, from: Expression<number>, length?: Expression<number> | undefined) => Operation<string>;
